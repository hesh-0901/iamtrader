(()=>{
const KEY='iamtrader:v1', S=()=>window.IAMTRADER?.state;
const save=()=>localStorage.setItem(KEY,JSON.stringify(S()));
const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
const ico=(n,s=24)=>{const p={wallet:'<path d="M4 7.5A2.5 2.5 0 0 1 6.5 5H19a2 2 0 0 1 2 2v11H6a2 2 0 0 1-2-2Z"/><path d="M4 8h14a3 3 0 0 1 3 3v2h-5a2 2 0 1 1 0-4h5"/>',shield:'<path d="M12 3 20 6v6c0 5-3.3 8-8 9-4.7-1-8-4-8-9V6Z"/><path d="m8.5 12 2.2 2.2 4.8-5"/>',chart:'<path d="M4 19V5M4 19h16"/><path d="m7 15 3-4 3 2 5-7"/>',settings:'<path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.64 5.64l2.12 2.12M16.24 16.24l2.12 2.12M18.36 5.64l-2.12 2.12M7.76 16.24l-2.12 2.12"/><circle cx="12" cy="12" r="3.5"/>',plus:'<path d="M12 5v14M5 12h14"/>',box:'<rect x="4" y="4" width="16" height="16" rx="3"/><path d="M8 12h8M12 8v8"/>',trash:'<path d="M4 7h16M9 7V4h6v3M7 7l1 13h8l1-13"/><path d="M10 11v5M14 11v5"/>'};return `<svg class="ui-icon" width="${s}" height="${s}" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">${p[n]||p.box}</svg>`};
function modal(html,cls=''){const m=document.createElement('div');m.className='modal';m.innerHTML=`<div class="modal-box small-modal ${cls}">${html}</div>`;document.body.append(m);m.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>m.remove());return m}
function renderAccounts(){const s=S();const list=s.accounts||[];document.querySelector('.content').innerHTML=`<div class="page-head"><div><span class="eyebrow">GESTION DU CAPITAL</span><h2>Comptes</h2><p>Centralisez vos comptes de trading et leurs règles de risque.</p></div><button class="btn green" data-account-new>${ico('plus',17)} Ajouter un compte</button></div><div class="account-hero-grid"><div class="account-overview card"><div class="account-overview-icon">${ico('wallet',28)}</div><div><span class="metric-label">COMPTE ACTIF</span><h3>${s.accounts.find(a=>a.id===s.activeAccountId)?.name||'Aucun compte sélectionné'}</h3><p>${s.accounts.find(a=>a.id===s.activeAccountId)?.firm||'Créez votre premier compte pour commencer.'}</p></div></div><div class="account-stat card"><span>Comptes</span><b>${list.length}</b><small>${list.filter(a=>a.id===s.activeAccountId).length?'1 actif':'Aucun actif'}</small></div><div class="account-stat card"><span>Capital sous suivi</span><b>${list.reduce((n,a)=>n+Number(a.capital||0),0).toLocaleString('fr-FR',{minimumFractionDigits:0,maximumFractionDigits:0})}</b><small>Capital initial cumulé</small></div></div><div class="accounts-list">${list.length?list.map(a=>accountCard(a,s.activeAccountId)).join(''):`<div class="feature-empty card">${ico('wallet',34)}<b>Aucun compte de trading</b><span>Ajoutez un compte réel, démo ou prop firm. Ses limites seront ensuite utilisées dans le cockpit.</span><button class="btn green" data-account-new>${ico('plus',16)} Créer mon premier compte</button></div>`}</div>`;document.querySelectorAll('[data-account-new]').forEach(b=>b.onclick=()=>accountModal());document.querySelectorAll('[data-account-edit]').forEach(b=>b.onclick=()=>accountModal(b.dataset.accountEdit));document.querySelectorAll('[data-account-active]').forEach(b=>b.onclick=()=>{s.activeAccountId=b.dataset.accountActive;save();renderAccounts()});document.querySelectorAll('[data-account-delete]').forEach(b=>b.onclick=()=>deleteAccount(b.dataset.accountDelete));}
function accountCard(a,active){return `<div class="account-card card ${active===a.id?'selected':''}"><div class="account-card-top"><div class="account-brand">${ico('wallet',25)}</div><div class="account-title"><div><span class="badge ${active===a.id?'buy':''}">${active===a.id?'ACTIF':'INACTIF'}</span><h3>${esc(a.name)}</h3><small>${esc(a.firm||'Compte personnel')} · ${esc(a.type||'Trading')}</small></div><button class="icon-btn" data-account-edit="${a.id}">${ico('settings',17)}</button></div></div><div class="account-metrics"><div><span>Capital initial</span><b>${Number(a.capital||0).toLocaleString('fr-FR')} ${esc(a.currency||'USD')}</b></div><div><span>Perte journalière max</span><b>${Number(a.maxDailyLoss||0).toFixed(2)}%</b></div><div><span>Drawdown max</span><b>${Number(a.maxDD||0).toFixed(2)}%</b></div><div><span>Objectif</span><b>${Number(a.targetPct||0).toFixed(2)}%</b></div></div><div class="account-card-actions">${active!==a.id?`<button class="btn small green" data-account-active="${a.id}">Activer ce compte</button>`:'<span class="status-good">'+ico('shield',15)+' Compte utilisé par le cockpit</span>'}<button class="btn small danger" data-account-delete="${a.id}">${ico('trash',14)} Supprimer</button></div></div>`}
function accountModal(id){const s=S(),a=s.accounts.find(x=>x.id===id)||{},edit=!!id;m=modal(`<div class="modal-head"><div><span class="eyebrow">${edit?'MODIFIER':'NOUVEAU'} COMPTE</span><h2>${edit?'Modifier le compte':'Ajouter un compte'}</h2><p>Une fiche simple : identité, capital et limites de risque.</p></div><button class="icon-btn" data-close>×</button></div><form id="accountForm"><div class="account-form-section"><b>Identité du compte</b><div class="field-grid two"><div class="field"><label>Nom du compte</label><input name="name" required placeholder="Ex. FundedNext 10K" value="${esc(a.name)}"></div><div class="field"><label>Broker / prop firm</label><input name="firm" placeholder="Ex. FundedNext" value="${esc(a.firm)}"></div><div class="field"><label>Type</label><select name="type"><option ${a.type==='Prop Firm'?'selected':''}>Prop Firm</option><option ${a.type==='Personnel'?'selected':''}>Personnel</option><option ${a.type==='Démo'?'selected':''}>Démo</option></select></div><div class="field"><label>Devise</label><select name="currency"><option ${a.currency==='USD'||!a.currency?'selected':''}>USD</option><option ${a.currency==='EUR'?'selected':''}>EUR</option><option ${a.currency==='GBP'?'selected':''}>GBP</option></select></div></div></div><div class="account-form-section"><b>Capital & règles</b><div class="field-grid three"><div class="field"><label>Capital initial</label><input name="capital" type="number" min="0" step="0.01" required value="${esc(a.capital||'')}"></div><div class="field"><label>Perte journalière max <em>%</em></label><input name="maxDailyLoss" type="number" min="0" step="0.01" value="${esc(a.maxDailyLoss||5)}"></div><div class="field"><label>Drawdown max <em>%</em></label><input name="maxDD" type="number" min="0" step="0.01" value="${esc(a.maxDD||10)}"></div><div class="field"><label>Objectif <em>%</em></label><input name="targetPct" type="number" min="0" step="0.01" value="${esc(a.targetPct||10)}"></div></div></div><div class="account-rule-note">${ico('shield',18)} <div><b>Ces limites servent au cockpit.</b><span>Elles alimentent le contrôle du risque, les alertes et les indicateurs de performance. Elles ne modifient pas automatiquement vos ordres.</span></div></div><div class="modal-actions"><button type="button" class="btn ghost" data-close>Annuler</button><button class="btn green">${ico('shield',16)} ${edit?'Enregistrer':'Créer le compte'}</button></div></form>`,'account-modal');const f=m.querySelector('#accountForm');f.onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(f));const obj={...a,id:id||crypto.randomUUID(),name:d.name,firm:d.firm,type:d.type,currency:d.currency,capital:Number(d.capital),maxDailyLoss:Number(d.maxDailyLoss),maxDD:Number(d.maxDD),targetPct:Number(d.targetPct)};if(edit)s.accounts=s.accounts.map(x=>x.id===id?obj:x);else{s.accounts.push(obj);s.activeAccountId=obj.id}s.activeAccountId=s.activeAccountId||obj.id;save();m.remove();renderAccounts()}}
function deleteAccount(id){const s=S(),a=s.accounts.find(x=>x.id===id);if(!a)return;if(!confirm(`Supprimer « ${a.name} » ? Les trades associés resteront enregistrés mais ne seront plus rattachés à un compte actif.`))return;s.accounts=s.accounts.filter(x=>x.id!==id);if(s.activeAccountId===id)s.activeAccountId=s.accounts[0]?.id||null;save();renderAccounts()}
function renderSettings(){const s=S(),custom=s.customInstruments||[];document.querySelector('.content').innerHTML=`<div class="page-head"><div><span class="eyebrow">CENTRE DE CONFIGURATION</span><h2>Paramètres</h2><p>Configurez IAMTRADER par modules. Chaque réglage reste à portée de main sans encombrer l’écran.</p></div></div><div class="settings-v3"><button class="setting-card card" data-settings="profile"><span class="setting-icon blue">${ico('settings',25)}</span><span><b>Profil & interface</b><small>Préférences générales, identité affichée et expérience du cockpit.</small></span>${ico('arrow',18)}</button><button class="setting-card card" data-settings="rules"><span class="setting-icon green">${ico('shield',25)}</span><span><b>Règles de trading</b><small>Cadre de risque, limites et discipline de votre compte actif.</small></span>${ico('arrow',18)}</button><button class="setting-card card" data-settings="assets"><span class="setting-icon orange">${ico('chart',25)}</span><span><b>Instruments</b><small>Actifs intégrés + création manuelle de vos propres instruments.</small></span>${ico('arrow',18)}</button><button class="setting-card card" data-settings="data"><span class="setting-icon purple">${ico('box',25)}</span><span><b>Données & sauvegarde</b><small>Vos données restent locales dans cette version et peuvent être exportées.</small></span>${ico('arrow',18)}</button></div><div class="settings-footer card"><div>${ico('shield',20)}<div><b>IAMTRADER garde une logique simple</b><span>Configurer une fois. Trader ensuite sans friction.</span></div></div></div>`;document.querySelectorAll('[data-settings]').forEach(b=>b.onclick=()=>settingsModal(b.dataset.settings));}
function settingsModal(kind){const s=S(),a=s.accounts.find(x=>x.id===s.activeAccountId);if(kind==='assets')return assetModal();if(kind==='rules')return accountModal(a?.id);const titles={profile:['Profil & interface','Les préférences qui changent votre expérience.'],data:['Données & sauvegarde','La gestion de vos données IAMTRADER.']};const [title,sub]=titles[kind];modal(`<div class="modal-head"><div><span class="eyebrow">PARAMÈTRE</span><h2>${title}</h2><p>${sub}</p></div><button class="icon-btn" data-close>×</button></div><div class="feature-empty">${ico(kind==='profile'?'settings':'box',34)}<b>${kind==='profile'?'Profil et personnalisation':'Données & sauvegarde'}</b><span>Ce module est préparé pour recevoir ses réglages dédiés sans alourdir la page principale.</span></div><div class="modal-actions"><button class="btn ghost" data-close>Fermer</button></div>`,'small-modal')}
function assetModal(){const s=S(),custom=s.customInstruments||[];const m=modal(`<div class="modal-head"><div><span class="eyebrow">INSTRUMENTS</span><h2>Vos actifs</h2><p>Utilisez les actifs standards ou ajoutez manuellement ceux de votre broker.</p></div><button class="icon-btn" data-close>×</button></div><div class="asset-add-head"><b>Actifs personnalisés</b><button class="btn small green" data-new-asset>${ico('plus',14)} Ajouter un actif</button></div><div class="custom-assets">${custom.length?custom.map(x=>`<div class="custom-asset"><span>${ico('chart',18)}</span><div><b>${esc(x.symbol)}</b><small>${esc(x.name||'Actif personnalisé')} · ${esc(x.quoteCurrency||'USD')}</small></div><em>${x.contractSize||'—'} / lot</em><button class="icon-btn" data-delete-asset="${esc(x.symbol)}">${ico('trash',15)}</button></div>`).join(''):'<div class="empty-mini">Aucun actif personnalisé.</div>'}</div><div class="asset-standard"><b>Actifs intégrés</b><div class="instrument-grid">${Object.values(window.IAMTRADER?.engines?.instruments||{}).map(x=>`<div class="instrument-card"><span>${ico('chart',17)}</span><div><b>${esc(x.symbol)}</b><small>${esc(x.name)}</small></div><em>${x.contractSize}</em></div>`).join('')||'<span class="muted">Actifs standards disponibles dans le moteur.</span>'}</div></div><div class="modal-actions"><button class="btn ghost" data-close>Fermer</button></div>`,'asset-modal');m.querySelector('[data-new-asset]').onclick=()=>{m.remove();newAssetModal()};m.querySelectorAll('[data-delete-asset]').forEach(b=>b.onclick=()=>{if(confirm(`Supprimer ${b.dataset.deleteAsset} ?`)){s.customInstruments=(s.customInstruments||[]).filter(x=>x.symbol!==b.dataset.deleteAsset);save();m.remove();assetModal()}})}
function newAssetModal(){const m=modal(`<div class="modal-head"><div><span class="eyebrow">ACTIF PERSONNALISÉ</span><h2>Ajouter un actif</h2><p>Renseignez les paramètres utilisés pour les calculs de risque.</p></div><button class="icon-btn" data-close>×</button></div><form id="assetForm"><div class="field-grid two"><div class="field"><label>Symbole</label><input name="symbol" required placeholder="Ex. BTCUSD"></div><div class="field"><label>Nom</label><input name="name" required placeholder="Ex. Bitcoin / Dollar"></div><div class="field"><label>Type</label><select name="type"><option>Forex</option><option>Index</option><option>Crypto</option><option>Matière première</option><option>Action</option><option>Autre</option></select></div><div class="field"><label>Devise de cotation</label><input name="quoteCurrency" value="USD" required></div><div class="field"><label>Contract size</label><input name="contractSize" type="number" step="any" min="0" required placeholder="100"></div><div class="field"><label>Tick size</label><input name="tickSize" type="number" step="any" min="0" required placeholder="0.01"></div><div class="field"><label>Pip size</label><input name="pipSize" type="number" step="any" min="0" required placeholder="0.01"></div><div class="field"><label>Lot step</label><input name="lotStep" type="number" step="any" min="0" value="0.01" required></div><div class="field"><label>Lot minimum</label><input name="minLot" type="number" step="any" min="0" value="0.01" required></div></div><div class="account-rule-note">${ico('chart',18)} <div><b>Pourquoi ces paramètres ?</b><span>IAMTRADER les utilise pour convertir la distance du stop en risque monétaire et calculer la taille de position.</span></div></div><div class="modal-actions"><button type="button" class="btn ghost" data-close>Annuler</button><button class="btn green">${ico('plus',16)} Ajouter l’actif</button></div></form>`,'asset-modal');m.querySelector('#assetForm').onsubmit=e=>{e.preventDefault();const d=Object.fromEntries(new FormData(e.target)),sym=d.symbol.trim().toUpperCase(),s=S();s.customInstruments=s.customInstruments||[];if(s.customInstruments.some(x=>x.symbol===sym)||['XAUUSD','EURUSD','GBPUSD','USDJPY','NAS100','US30','SPX500','XAGUSD'].includes(sym))return alert('Ce symbole existe déjà.');s.customInstruments.push({symbol:sym,name:d.name.trim(),type:d.type,quoteCurrency:d.quoteCurrency.toUpperCase(),contractSize:Number(d.contractSize),tickSize:Number(d.tickSize),pipSize:Number(d.pipSize),lotStep:Number(d.lotStep),minLot:Number(d.minLot)});save();m.remove();assetModal()}}
function route(){const s=S();if(!s)return;if(s.page==='accounts'&&!document.querySelector('.account-hero-grid'))renderAccounts();if(s.page==='settings'&&!document.querySelector('.settings-v3'))renderSettings()}
const o=new MutationObserver(route);o.observe(document.body,{childList:true,subtree:true});window.addEventListener('load',route);setTimeout(route,80);
})();
