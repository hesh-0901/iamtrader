const $ = (id) => document.getElementById(id);
const form = $('tradeForm');
const fields = ['entry','stopLoss','takeProfit','quantity','contractMultiplier','accountBalance'];
function values(){return Object.fromEntries(fields.map(id=>[id,Number($(id).value)]))}
function formatMoney(n){return Number.isFinite(n)?`$${n.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}`:'—'}
function formatNum(n){return Number.isFinite(n)?n.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:4}):'—'}
function calculate(v){if(![v.entry,v.stopLoss,v.takeProfit,v.quantity,v.contractMultiplier,v.accountBalance].every(Number.isFinite))throw new Error('Tous les champs numériques sont requis.');if(v.quantity<=0||v.contractMultiplier<=0||v.accountBalance<=0)throw new Error('Quantity, contract multiplier et solde doivent être supérieurs à 0.');const riskDistance=Math.abs(v.entry-v.stopLoss);if(riskDistance===0)throw new Error('Le Stop Loss doit être différent de l’Entry.');const riskAmount=riskDistance*v.quantity*v.contractMultiplier;return{riskAmount,riskPercent:riskAmount/v.accountBalance*100,rr:Math.abs(v.takeProfit-v.entry)/riskDistance}}
function preview(){try{const v=values(),r=calculate(v);$('rr').textContent=`${formatNum(r.rr)}R`;$('riskAmount').textContent=formatMoney(r.riskAmount);$('riskPercent').textContent=`${formatNum(r.riskPercent)}%`;$('riskFormula').textContent=formatNum(Math.abs(v.entry-v.stopLoss));$('rewardFormula').textContent=formatNum(Math.abs(v.takeProfit-v.entry));$('accountBalanceLabel').textContent=formatMoney(v.accountBalance)}catch{$('rr').textContent='—';$('riskAmount').textContent='—';$('riskPercent').textContent='—';$('riskFormula').textContent='Entry ↔ SL';$('rewardFormula').textContent='Entry ↔ TP'}$('directionBox').textContent=$('direction').value==='BUY'?'BUY · Long exposure':'SELL · Short exposure'}
fields.concat(['direction']).forEach(id=>$(id).addEventListener('input',preview));
function render(){const trades=JSON.parse(localStorage.getItem('iamtrader_trades')||'[]'),body=$('tradeRows');if(!trades.length){body.innerHTML='<tr><td colspan="8" class="empty">Aucune opération enregistrée.</td></tr>';return}body.innerHTML=trades.map(t=>`<tr><td>${new Date(t.dateTime).toLocaleString('fr-FR')}</td><td>${t.symbol}</td><td class="${t.direction==='BUY'?'side-buy':'side-sell'}">${t.direction}</td><td>${formatNum(t.entry)}</td><td>${formatNum(t.stopLoss)}</td><td>${formatNum(t.takeProfit)}</td><td>${formatNum(t.rr)}R</td><td>${formatMoney(t.riskAmount)}</td></tr>`).join('')}
form.addEventListener('submit',e=>{e.preventDefault();$('error').hidden=true;try{const v=values(),r=calculate(v),payload={symbol:$('symbol').value.trim().toUpperCase(),direction:$('direction').value,...v,dateTime:$('dateTime').value,note:$('note').value.trim(),...r};if(!payload.symbol)throw new Error('Asset / Symbol est requis.');const trades=JSON.parse(localStorage.getItem('iamtrader_trades')||'[]');trades.unshift(payload);localStorage.setItem('iamtrader_trades',JSON.stringify(trades.slice(0,100)));render();form.reset();$('contractMultiplier').value='1';$('accountBalance').value='10000';$('dateTime').value=new Date().toISOString().slice(0,16);preview()}catch(err){$('error').textContent=err.message;$('error').hidden=false}});
$('clearTrades').addEventListener('click',()=>{localStorage.removeItem('iamtrader_trades');render()});$('dateTime').value=new Date().toISOString().slice(0,16);preview();render();